{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "region": "sa-east-1",
      "source_ami": "ami-09f4cd7c0b533b081",
      "instance_type": "t2.micro",
      "ssh_username": "ubuntu",
      "ami_name": "laa-{{timestamp}}"
    }
  ]
}